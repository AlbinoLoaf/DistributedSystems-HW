syntax = "proto3";

package hw05;

option go_package = "grpc/proto";

message bid{
    int64 bid = 1; 
}

message serverReply{
    bool Succes =1;

}


service Auction{
    rpc sendBid (bid) returns (serverReply);
}